<div class="max-w-3xl mx-auto px-4">
  <div class="flex items-center justify-between">
    <h1 class="flex-1 font-bold text-3xl">Genre</h1>

    <div class="flex-wrap lg:flex items-center lg:space-x-3">
      <%= link_to genres_path, class: button_class(theme: "transparent") do %>
      All Genres
      <% end %>
      <%= link_to edit_genre_path(@genre), class: button_class(theme:"primary") do %>
      Edit
      <% end %>
    </div>
  </div>

  <ul class="text-slate-700">
      <li>
        <p class="font-semibold">Genreid:</p>
        <p><%= @genre.genreid %></p>
      </li>
      <li>
        <p class="font-semibold">Genre name:</p>
        <p><%= @genre.genre_name %></p>
      </li>
  </ul>
</div>
